// var prenom = "chems";
// var age = "24";

// var prenomLogin = window.prompt("comment tu t'appelle?")

// var age = window.prompt("ton age?");

// Comparaison de deux nombres
// Ecrivez un programme comparaison.js qui fait saisir deux nombres puis compare leurs valeurs et affiche un message approprié (plus grand, plus petit, égal).

// 1- Afficher deux champs de formulaires à l'aide de la fonction prompt
// 2- Convertir les valeurs de retour des fonctions prompt aux valeurs numérique, grâce à la fonction "parseInt"
// 3- Affecter les valeurs de retours, chacune dans une variable : num1, num2
// 4- La partie comparaison : c'est à vous de développer

// var nmb1 =  parseInt(window.prompt("Inserez la 1er valeur"));
// var nmb2 =  parseInt(window.prompt("Inserez la 2eme valeur"));

// if (nmb1 == nmb2) {
//     document.write("egaux");
// } else if (nmb1 > nmb2) {
//     document.write (nmb1 + " est superieur a " + nmb2)
// } else {
//     document.write (nmb1 + " est inferieur a " + nmb2 )
// }
// la fonction"isNan"  il prend un parametre ( ici num1) et verifie si le type de la variable "number" ou "string". Apres la verification, elle retourne un boolean: true ou false.
// si le type du parametre est "string", la fonction true, sinon ellle retourne false

console.log(isNaN(num1));

